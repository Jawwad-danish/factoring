export enum BrokerRating {
  A = 'A',
  B = 'B',
  C = 'C',
  D = 'D',
  F = 'F',
  X = 'X',
  NoRating = '--',
}

export enum FlaggedEntity {
  Invoice = 'Invoice',
  Broker = 'Broker',
  Client = 'Client',
}

export enum BrokerAuthorityStatus {
  Active = 'Active',
  Inactive = 'Inactive',
}
